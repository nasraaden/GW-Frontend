const initialState = {
    score: 0
}

export const Reducer = (state = initialState, action) => {

    switch(action.type) {
        case 'UPDATE_SCORE':
            break;
        default:
            return state;
    }
}